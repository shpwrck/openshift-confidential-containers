# Secrets & hardware-bound artifacts — never commit
pull-secret.json
auth.json
# local Codex CLI config — may hold MCP bearer tokens (e.g. latitudesh)
.codex/
*.der
*.pem
adminPrivateKey
publicKey
vcek-bundle/
rvps-*.yaml
initdata-*.toml
rung-bc-artifacts/
cosign.key
cosign.pub
*.tmp

# Fetched tooling + install working dirs (large, regenerable — never commit)
bin/
cluster-assets/
mirror/

# Ansible — runtime-generated + local secret/vars files (never commit)
ansible/group_vars/discovered.yml
ansible/group_vars/secrets.yml
ansible/group_vars/*.local.yml
ansible/inventory/*.local.yml
ansible/*.retry
ansible/.vault-pass

# Terraform
infra/**/.terraform/
infra/**/terraform.tfvars
infra/**/*.tfstate
infra/**/*.tfstate.*
infra/**/crash.log
infra/**/.env
.env
infra/latitude/IPMI-ACCESS.md
