---
galaxy_info:
  role_name: bastion_egress
  author: coco-rig
  description: Harden a fresh Latitude bastion's egress for oc-mirror (drop IPv6 default, clamp MTU + SYN MSS).
  license: Apache-2.0
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions:
        - "9"
        - "10"
  galaxy_tags:
    - openshift
    - airgap
dependencies: []
