---
galaxy_info:
  role_name: mirror_push
  author: coco-rig
  description: oc-mirror v2 push to the local mirror registry with resume-on-retry.
  license: Apache-2.0
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions:
        - "9"
        - "10"
  galaxy_tags:
    - openshift
    - airgap
dependencies: []
