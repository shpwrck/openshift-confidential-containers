apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Demo workloads per rung: a) secret-resource pod, b) encrypted image, c) signed image.
# initdata is environment-bound (HOST_DATA) — kept per-overlay, not here.
resources:
  - rung-a-secret-pod.yaml
