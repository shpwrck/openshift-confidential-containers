apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Operator subscriptions + namespaces. Install order: NFD -> cert-manager -> OSC -> Trustee.
# Populate from onboarding guide Gates 2-4 + Step 3.
resources: []
  # - namespaces.yaml
  # - nfd-subscription.yaml
  # - cert-manager-subscription.yaml
  # - osc-subscription.yaml
  # - trustee-subscription.yaml
